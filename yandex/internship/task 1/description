При решении данной задачи воспользуемся алгоритмом Эратосфена. Идея алгоритма заключается в вычеркивании составных чисел,
в частности, заменой их нулями, которые в дальнейшем будут удалены из массива.
Для начала сформируем список из всех цифр в диапазоне от 0 до запрашиваемого числа (назовем его N). После этого отбросим
единицу, занулив второй (с индексом 1) элемент массива. Делается это с целью того, чтобы избежать единицы, которая не
считается простым числом.
Цикл начнем с двойки, так как первые два элемента нас не интересуют.

Если число не забито нулем, наша задача вычеркнуть его